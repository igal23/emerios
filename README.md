# EMERIOS

## Compilación
Para compilar la aplicación se debe utilizar el framework dotnet 7.0 y utilizar el comando `dotnet build`

## Ejecución
Para ejecutar el código, se debe utilizar el comando `dotnet run`. El resultado será devuelto por línea de comandos.

Para cambiar la cadena de entrada, modifique el archivo input.txt